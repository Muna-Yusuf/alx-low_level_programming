#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs -  computes the absolute value of an integer.
 * @i: for the int
 * Return:0 if doen
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);

}
