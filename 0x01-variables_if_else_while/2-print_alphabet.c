#include<stdio.h>
/**
 * main-random number and last digit
 * Return:0(if done)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
